# Logical transition functions

Use a transition only when it names a relation:

- result → consequence;
- definition → operational criterion;
- observation → explanatory model;
- general principle → special case;
- assumption → limitation;
- formal relation → physical interpretation;
- theory → worked application;
- local conclusion → next unresolved question.

Avoid generic transitions that could be inserted anywhere.
