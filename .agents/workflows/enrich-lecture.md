# Enrich lecture methodology

1. Read `input/lecture_config.md`, the blueprint, briefs and completed section files.
2. Activate `methodical-enrichment` and `document-numbering`.
3. Create `output/methodical_inserts.json` only; do not edit section files.
4. Cover configured learning functions with concise thematic examples, memory aids, formula-reading notes, common-error warnings and self-checks.
5. Keep factual inserts linked to supported claims/evidence. Mark illustrative values as hypothetical.
6. Validate `contracts/methodical-inserts.schema.json` and run the methodical cross-reference checks.
7. Hand the artifact to the coherence editor for insertion into the draft.
